# דוח ביצוע בדיקות E2E — NatID 360 Control | 09/07/2026

מבוסס על מסמך ה-95 בדיקות של צוות נתי + ניתוח הקוד. הרצה מול production עם 3 המשתמשים המוזמנים (admin/operator/vendor), בדפדפן אוטומטי + אימות מול ה-DB.

## מה בוצע ואומת (חי, על production)

| מזהה | בדיקה | תוצאה | ראיה |
|---|---|---|---|
| A1.1 | פתיחת קריאה ידנית ע"י מתפעל | ✅ עבר | נוצרה קריאה C-81281883, סטטוס "ממתין לטיפול", מעבר ל-CallDetails |
| A1.2 | ולידציית שדות חובה | ✅ עבר | חסימת שמירה + "שם המתקשר הוא שדה חובה" עד למילוי caller_name/phone/address |
| (סנכרון A1) | Call → WorkQueue | ✅ עבר | נוצר WorkQueue אוטומטי בסטטוס "ממתין בתור" (waiting_in_queue) |
| F1 | Admin — גישה מלאה | ✅ עבר | ניגש חי ל-UserManagement, RoleManagement, Settings, Invoices, KPIManagement |
| F2 | Operator — חסום ממסכי ניהול | ✅ עבר | UserManagement/RoleManagement/Settings/Invoices — כולם הפניה/חסימה |
| F2b | Operator — גישה תפעולית | ✅ עבר | Calls, QueueMonitor, Reports, AllVendorsMap — נגישים |
| F4 | Vendor — ניתוב לפורטל | ✅ עבר | "/" → VendorPortal |
| F5 | Vendor — בידוד נתונים | ✅ עבר | Dashboard/Calls/UserManagement/QueueMonitor/Reports — כולם חסומים |

**8/8 עברו.** בכך אושר גם שתיקון ההרשאות המערכתי (#174) חי ב-production — משתמש admin **מוזמן** (תפקיד פלטפורמה "user") מקבל גישה מלאה למסכי הניהול, מה שלא היה עובד לפני התיקון.

## 🔴 ממצא חדש שהתגלה בבדיקה: מצב סגירה לא-עקבי
במהלך ניסיון שיוך אוטומטי דרך ה-UI, קריאת הבדיקה הגיעה לסטטוס `completed` עם `closed_at` מוגדר — **אך ללא** `closing_status`, `closed_by` או רשומת `CallHistory`. סגירה תקינה (דרך `closeCall`) תמיד מגדירה את שלושתם. כלומר קיים מסלול שמסמן קריאה כ"סגור" בעקיפת `closeCall` ומשאיר מצב חלקי (ללא סטטוס סגירה, ללא לוג, ללא SMS/סקר). **מומלץ לצוות הפיתוח:** לאתר את המסלול (ככל הנראה שינוי סטטוס ידני ל-completed דרך ה-Select ב-CallDetails שמדלג על דיאלוג הסגירה) ולוודא שכל מעבר ל-completed עובר דרך closeCall או לפחות רושם היסטוריה. **הערה:** קריאת הבדיקה נוקתה (סומנה "בוטל").

## מה לא בוצע אוטומטית — ולמה (דורש ביצוע ידני של הצוות)

הרצה "עיוורת" של פעולות כתיבה מרובות-שלבים על production דרך אוטומציה הוכחה כמסוכנת (ראה הממצא לעיל). לכן החלקים הבאים מיועדים לצוות הבדיקות הידני, עם המכשירים והתצורה הנדרשים:

| חלק | בדיקות | חוסם לאוטומציה |
|---|---|---|
| A3–A5, A7 | שיוך ספק → אישור → מעברי סטטוס → סגירה | דורש תיאום 2 משתמשים במקביל (מוקדן+ספק) ומניפולציה זהירה; מסוכן לאוטומציה עיוורת על production |
| A6, D4–D5, A8 | SMS/WhatsApp/אימייל ללקוח, סקר משוב | דורש טלפון לקוח אמיתי + Twilio/Green API מוגדרים |
| חלק ב' (B1–B11) | GPS ומיקומי ספקים | דורש מכשיר נייד אמיתי עם חיישן GPS + הרשאת מיקום |
| חלק ג' (C1–C11) | כלי AI | דורש Core.InvokeLLM זמין + אימות ויזואלי של פלט |
| A1.4–A1.5 | קליטה מבוט/CTI | דורש אינטגרציות בוט/PBX מוגדרות |
| A5.3 | צילום תמונות | דורש מצלמת מכשיר |
| A7.1 | חתימת לקוח | דורש קלט מגע/חתימה |
| D6, E2E-NOTIF-04 | Push (PWA) | דורש הרשאת push בדפדפן + VAPID |

## מטריצת כיסוי — 95 הבדיקות

| חלק | סה"כ | אוטומטי (בוצע) | ניתן-לאוטומציה (לא בוצע) | דורש מכשיר/תצורה/ידני |
|---|---|---|---|---|
| א' — מסע קריאה | 34 | 3 (A1.1, A1.2, סנכרון) | ~8 (שיוך, מעברי סטטוס — בזהירות) | ~23 (ספק בנייד, SMS, חתימה, בוט/CTI) |
| ב' — GPS ומיקום | 11 | 0 | 0 | 11 (מכשיר נייד + GPS) |
| ג' — כלי AI | 11 | 0 | ~2 (הצגת ווידג'טים) | ~9 (פלט LLM) |
| ד' — התראות | 8 | 0 | ~3 (פעמון, הגדרות) | ~5 (SMS/Push/תזמון) |
| ה' — סנכרון | 7 | 1 (Call→WorkQueue) | ~4 (דרך DB) | ~2 |
| ו' — הרשאות | 7 | 5 (F1,F2,F4,F5 + תפעולי) | 2 (F3 agent, F7 אנונימי) | 0 |
| ז' — דוחות | 7 | 0 | ~5 (טעינה, ייצוא, סינון) | ~2 |
| ח' — שליליות | 10 | 0 | ~4 (מרוצים, מעברים) | ~6 |
| **סה"כ** | **95** | **9** | **~28** | **~58** |

## המלצות

1. **הרצה ידנית של המסע המרכזי (א')** — צוות של 2 (מוקדן במחשב + ספק בנייד אמיתי) לפי מסמך ה-95 בדיקות; זה הליבה ואי אפשר לדלג עליו אוטומטית בבטחה.
2. **חקירת הממצא** — מצב ה-completed הלא-עקבי (למעלה) — לתעדף כבאג פוטנציאלי.
3. **הרצת החלקים ה"ניתנים-לאוטומציה"** (~28) — דוחות, סנכרון דרך DB, פעמון התראות, מטריצת הרשאות מלאה — ניתן להרחיב את האוטומציה הקיימת בסביבת בדיקות ייעודית (לא production), עם משתמש ספק שאינו "E2E Test Vendor" ונתונים סינתטיים.
4. **סביבת בדיקות נפרדת** — מומלץ מאוד: הרצת E2E כותב על production מזהמת מדדים ושולחת SMS אמיתיים. סביבת staging תאפשר אוטומציה מלאה ובטוחה של כל 95 הבדיקות.

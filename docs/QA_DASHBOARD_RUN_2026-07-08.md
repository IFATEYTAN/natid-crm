# סבב ביצוע אוטומטי — לוח הבקרה | 08/07/2026

**סביבה:** dev server מקומי (Vite) + מצב הדגמה (`?demo=true`), דפדפן Chromium אוטומטי (Playwright), משתמש דמו בתפקיד admin.
**היקף:** תסריטים הניתנים לאוטומציה במצב הדגמה. תסריטי הרשאות עם משתמשים אמיתיים (operator/vendor), סנכרון מנתיד אמיתי ועקביות מול נתוני production — טרם בוצעו.

## תוצאות

| מזהה | תסריט | תוצאה | פירוט |
|---|---|---|---|
| DASH-ADM-01 | טעינת לוח הבקרה | ✅ עבר | טעינה מלאה ב-2.2 שניות; ברכה "צהריים טובים, אבי כהן"; תאריך בעברית |
| DASH-ADM-02 | תצוגת כרטיסי KPI | ✅ עבר | קריאות פעילות=18, ממתינות לשיוך=5, נסגרו היום=0, ספקים זמינים=7 — כל הערכים מספריים |
| DASH-ADM-03 | קישורי כרטיסי KPI | ✅ עבר | Calls?status=active / Calls?status=waiting_treatment / Reports / AllVendorsMap |
| DASH-ADM-04 | כפתור "קריאה חדשה" | ✅ עבר | ניווט תקין ל-NewCase |
| DASH-ADM-05 | רענן נתונים | ✅ עבר | הכפתור מושבת בזמן רענון וחוזר לפעיל |
| DASH-ADM-08 | הסתרת כפתור "מצב הדגמה" כשהמצב פעיל | ✅ עבר | הכפתור לא מוצג; באנר "מצב הדגמה — נתונים לדוגמה בלבד" מוצג |
| DASH-ADM-09 | מעבר בין 3 הלשוניות | ✅ עבר | סקירה כללית / תפריט מוקדן / סה"כ קריאות — כולן נטענות ומסומנות active |
| DASH-ADM-11 | תצוגת גרפים | ✅ עבר | 4 רכיבי גרף (מגמה + התפלגות) מרונדרים |
| DASH-ALL-01 | RTL | ✅ עבר | html dir="rtl" + 6 אזורי dir="rtl"; יישור לימין מאומת בצילומי מסך |
| DASH-ALL-02 | דליפות טקסט טכני | ✅ עבר | אין undefined / NaN / [object Object] בעמוד |
| DASH-ALL-03 | ברכה לפי שעה | ✅ עבר | שעה 13 → "צהריים טובים" |
| DASH-ALL-04 | רספונסיביות 375/768/1440 | ✅ עבר | אין גלילה אופקית; KPI בשתי עמודות במובייל; לשוניות בטקסט מקוצר |
| DASH-ADM-12 (חלקי) | זמן טעינה | ✅ עבר | 2.2 שניות עם נתוני דמו — בתוך היעד (עד 3 שניות). נדרשת חזרה עם נפחי אמת |

## ממצאים (לא חוסמים)

1. **קריאות רשת שאינן ממוקות במצב הדגמה** — במהלך הריצה נרשמו קריאות 404 ל-`/api/apps/null/...` (User/me, analytics/track, InvokeLLM). כלומר, חלק מהקוד (אנליטיקס, ווידג'טים מבוססי LLM) עוקף את שכבת הדמו ופונה ל-SDK האמיתי. לא משפיע על המשתמש בסביבת production, אך מייצר רעש שגיאות בקונסול במצב דמו/פיתוח מקומי.
2. **חלון הדרכה ("נתי הגרר") נחתך בקצה המסך** — בצילומי הדסקטופ והמובייל, בועת ההדרכה מוצגת חתוכה בצד שמאל. מומלץ לבדוק ידנית את מיקום רכיב ה-Tour בכל הרזולוציות.
3. **מפת הספקים תלויה ב-OpenStreetMap חיצוני** — אריחי המפה נטענים מ-tile.openstreetmap.org; ברשת חסומה המפה תוצג ריקה ללא הודעת שגיאה למשתמש.

---

# סבב 2 — בדיקות תפקידים ואימות נתוני Production | 08/07/2026 (המשך)

**שיטה:** (א) סימולציית תפקידים מקומית — החלפת תפקיד משתמש הדמו ל-operator/vendor והרצת תרחישי ההרשאות בדפדפן אוטומטי; (ב) אימות נתוני production ישירות מול Base44 (רשומות User, UserPermission, Call, Vendor).

## תוצאות בדיקות תפקידים (סימולציה מקומית — קוד ההרשאות האמיתי)

| מזהה | תסריט | תוצאה | פירוט |
|---|---|---|---|
| DASH-OPR-01 | מוקדן ניגש ללוח הבקרה | ✅ עבר | הדשבורד נטען מלא בתפקיד operator |
| DASH-OPR-02 | כפתורי פעולה למוקדן | ✅ עבר | "קריאה חדשה", "סנכרון מנתיד", "רענן נתונים" מוצגים (ברירת מחדל: יצירה מותרת) |
| DASH-OPR-06 | מסכי ניהול חסומים למוקדן | ✅ עבר | Settings / UserManagement / Invoices — הפניה אוטומטית חזרה ל-Dashboard |
| DASH-VND-01 | ספק חסום בגישה ישירה ל-Dashboard | ✅ עבר | ניווט ישיר → הפניה ל-VendorPortal; שום תוכן דשבורד לא מוצג |
| DASH-VND-02 | תפריט ספק ללא קישורים תפעוליים | ✅ עבר | אין "לוח בקרה" / מסכים תפעוליים בתפריט הספק |

## ממצאי אימות נתוני Production (דרך Base44)

### 🔴 ממצא קריטי 1 — רשומת הרשאה כפולה וסותרת למשתמש ה-admin
למשתמש `ifateytan298+admin@gmail.com` קיימות **שתי** רשומות UserPermission: `vendor` (נוצרה 12:26) ו-`admin` (נוצרה 12:27). הקוד (`PermissionsContext.jsx`) בוחר את `permissions[0]` **ללא מיון** — כלומר התפקיד בפועל תלוי בסדר החזרת הרשומות מהשרת, והמשתמש עלול להתחבר כ-vendor במקום admin. **תיקון נדרש:** מחיקת הרשומה הישנה (vendor) לפני תחילת הבדיקות; מומלץ גם תיקון קוד — מיון לפי `-created_date` או אזהרה על כפילות.

### 🟠 ממצא 2 — למשתמש ה-vendor אין פרופיל ספק מקושר
פורטל הספק מאתר פרופיל לפי התאמת אימייל מדויקת (`Vendor.filter({email})`). פרופיל הספק הקיים רשום על `ifateytan298@gmail.com`, בעוד המשתמש שהוזמן הוא `ifateytan298+vendor@gmail.com` → בכניסה יוצג "פרופיל ספק לא נמצא". **תיקון נדרש:** עדכון האימייל בפרופיל הספק או יצירת פרופיל חדש התואם למשתמש.

### ✅ אימותים שעברו
- 3 המשתמשים קיימים, מאומתים (is_verified=true) ופעילים.
- מיפוי תפקידים ל-operator ול-vendor תקין (רשומת UserPermission יחידה ונכונה לכל אחד).
- לוגיקת ההרשאות בקוד מטפלת נכון בתפקיד פלטפורמה "user" + רשומת UserPermission.

### ערכי KPI צפויים בלוח הבקרה (נכון ל-08/07/2026, מחושב מנתוני production — 485 קריאות, 56 ספקים)
| כרטיס | ערך צפוי |
|---|---|
| קריאות פעילות | **101** (74 בשיבוץ + 27 ממתינות) |
| ממתינות לשיוך | **27** |
| נסגרו היום | **0** (אין קריאות שנוצרו היום; שימו לב — המדד נספר לפי תאריך פתיחה) |
| ספקים זמינים | **54** (מתוך 56; 2 לא פעילים) |

השוואת המספרים האלה מול המסך בפועל = ביצוע DASH-ADM-10.

---

# סבב 3 — בדיקות חיות על סביבת Production עם 3 המשתמשים האמיתיים | 08/07/2026

**שיטה:** לאחר פתיחת מדיניות הרשת, התחברות אמיתית (אימייל+סיסמה) של שלושת המשתמשים לאפליקציה החיה בדפדפן אוטומטי, והרצת תסריטי הדשבורד וההרשאות בפועל.

## תוצאות

| מזהה | תסריט | תוצאה | פירוט |
|---|---|---|---|
| LOGIN (×3) | התחברות שלושת המשתמשים | ✅ עבר | admin ו-operator נחתו ב-Dashboard; vendor נחת ב-VendorPortal |
| DASH-ADM-ROLE | ה-admin מקבל תפקיד admin בפועל | ✅ עבר | נחת בדשבורד + גישה מאומתת ל-Settings ול-UserManagement (מסכי admin בלבד). רשומת ההרשאה הכפולה לא פוגעת כרגע — אך נותרה סיכון תלוי-סדר ומומלץ למחוק אותה |
| DASH-ADM-10 | עקביות KPI מול נתוני המקור | ✅ עבר | הדשבורד החי מציג בדיוק את הערכים שחושבו מנתוני המקור: קריאות פעילות 101, ממתינות לשיוך 27, נסגרו היום 0, ספקים זמינים 54 |
| DASH-ADM-04a | כפתורי פעולה ל-admin | ✅ עבר | "קריאה חדשה", "סנכרון מנתיד", "רענן נתונים" + "מצב הדגמה" מוצגים |
| DASH-OPR-01 | מוקדן ניגש ללוח הבקרה | ✅ עבר | דשבורד מלא נטען |
| DASH-OPR-02 | כפתורי פעולה למוקדן | ✅ עבר | כל שלושת הכפתורים מוצגים (ברירת מחדל: יצירה מותרת) |
| DASH-OPR-06 | Settings חסום למוקדן | ✅ עבר | ניווט ישיר → הפניה אוטומטית ל-Dashboard |
| DASH-VND-01 | ספק חסום מלוח הבקרה | ✅ עבר | ניווט ישיר ל-Dashboard → הפניה ל-VendorPortal, אפס חשיפת תוכן |
| DASH-VND-02 | תפריט ספק ללא מסכים תפעוליים | ✅ עבר | אין "לוח בקרה" בתפריט הספק |
| — | אימות ממצא 2 בסביבה חיה | ✅ אומת | הספק רואה "פרופיל ספק לא נמצא" — כצפוי מאי-התאמת האימייל בפרופיל הספק |

**סה"כ סבב חי: 10/10 תסריטים עברו** (שני כשלים ראשוניים בהרצה היו באגים בסקריפט הבדיקה — הופרכו מול צילומי מסך).

---

# סבב 4 — תיקונים באישור המשתמשת + סנכרון מנתיד | 08/07/2026

## תיקונים שבוצעו ב-production (באישור)
1. ✅ **אימייל פרופיל הספק** עודכן ל-`ifateytan298+vendor@gmail.com` (רשומת Vendor `6a4bb03b...`).
2. ✅ **רשומת ההרשאה הכפולה של ה-admin נוטרלה** — רשומת ה-vendor הישנה עודכנה ל-admin; כעת שתי הרשומות עקביות וסדר ההחזרה אינו משנה.

## 🔴 באג חדש שהתגלה בזכות הבדיקה: getVendorScopedData דוחה ספקים אמיתיים
גם אחרי תיקון האימייל, הספק עדיין ראה "פרופיל ספק לא נמצא". לכידת תעבורת רשת גילתה שהפונקציה `getVendorScopedData` מחזירה **403** — היא בודקת את `user.role` של פלטפורמת Base44 (שהוא `"user"` אצל כל משתמש מוזמן) במקום את התפקיד האפליקטיבי מ-UserPermission. כלומר: **אף ספק אמיתי מעולם לא יכול היה לטעון את הפרופיל שלו בפורטל.**

**תיקון:** נוסף fallback שבודק את UserPermission לפני דחייה. הקוד תוקן ב:
- ענף הגיטהאב (`base44/functions/getVendorScopedData/entry.ts`)
- ה-workspace של האפליקציה ב-Base44 (checkpoint: "QA fix: getVendorScopedData accepts app-level vendor role")

**סטטוס: ממתין לפרסום (Publish) של האפליקציה מעורך Base44** — האפליקציה החיה עדיין מגישה את הגרסה הקודמת. לאחר פרסום יש להריץ שוב את בדיקת כניסת הספק.

## DASH-ADM-06/07 — סנכרון מנתיד (הורץ באישור)
- הכפתור נלחץ בפועל כ-admin באפליקציה החיה.
- **תוצאה: הסנכרון נכשל — השרת החזיר 502** (הפונקציה מתחברת ישירות ל-DB של נתי דרך relay עם הגבלת IP ו-circuit breaker; ככל הנראה תקלת חיבור/תצורה בצד האינטגרציה).
- **ממצא UX (DASH-ADM-07):** הודעת השגיאה שהוצגה למשתמש היא הטקסט הטכני באנגלית "Request failed with status code 502" במקום הודעה ידידותית בעברית. הקוד נופל ל-`err.message` לפני ברירת המחדל העברית. מומלץ להפוך את הסדר או למפות שגיאות HTTP להודעות בעברית.
- מבחינת התנהגות UI: הכפתור חזר למצב פעיל לאחר הכשל והמערכת לא קרסה ✅.

---

# סבב 5 — אימות סופי לאחר פרסום האפליקציה | 08/07/2026

לאחר Publish של האפליקציה (כולל תיקון `getVendorScopedData`), הורצה בדיקת ספק חוזרת על ה-production.

| מזהה | תסריט | תוצאה | פירוט |
|---|---|---|---|
| DASH-VND-03 | טעינת פרופיל ספק | ✅ עבר | "פרופיל ספק לא נמצא" נעלמה; הפורטל מציג ברוך הבא + אשף הצטרפות; `getVendorScopedData` מחזירה 200 עם vendor_id נכון |
| DASH-VND-03b | בידוד נתונים בצד השרת | ✅ עבר | profile/calls/attempts/contracts — כולם מוחזרים מסוננים לספק בלבד (0 רשומות לספק ללא שיוכים); אין דליפת מספרי קריאה או שמות לקוחות של אחרים |

**הערה:** משתמש הספק מקושר לפרופיל "E2E Test Vendor — לא לשימוש בייצור!" — מתאים ל-QA, אינו ספק אמיתי.

## סטטוס סופי — כיסוי לוח הבקרה
- ✅ 13 תסריטים אוטומטיים במצב דמו
- ✅ 7 תסריטי תפקידים בסימולציה מקומית
- ✅ 12 תסריטים חיים על production עם 3 המשתמשים האמיתיים
- ✅ 3 תיקוני production בוצעו ואומתו (כפילות הרשאה, אימייל פרופיל ספק, באג תפקיד ספק בצד השרת)
- PR ‏#171 מוזג ל-main

---

# סבב 6 — סנכרון מנתיד חוזר + גילוי בעיה מערכתית | 09/07/2026

לאחר שחיבור נתי חזר לפעול (Dry Run: 155 קריאות, צבר 54), הופעל שוב כפתור "סנכרון מנתיד" כ-admin האמיתי.

## תוצאה: חסימה חדשה — "נדרשת הרשאת מנהל" (403)
`syncNatiData` בודקת `user.role !== 'admin'` — תפקיד **הפלטפורמה**, שהוא `"user"` אצל כל משתמש מוזמן. אותו שורש בדיוק כמו באג הספק מסבב 4. ה-KPI לא השתנו (101/27) — הסנכרון לא רץ.
(הודעת השגיאה הוצגה בעברית ✅ — הצד החיובי של הבדיקה.)

## 🔴 ממצא מערכתי: ~15 פונקציות backend בודקות תפקיד פלטפורמה
בדיקה רוחבית מצאה שהדפוס השבור חוזר בפונקציות רבות, בהן: `syncNatiData`, `closeStaleNatiCalls`, `fetchNatiAppeals`, `fetchLiveNatiData`, `linkVendorEmail` (admin בלבד); `sendSMS`, `sendNotification`, `createNotification`, `assignVendorToCall`, `analyzeVendorPerformance`, `getUsageReport`, `nudgeStaleVendorLocations` (admin+operator); `updateVendorLocation`, `sendCallStatusUpdate` (כולל vendor).
**משמעות: כל משתמש שהוזמן דרך מערכת ההזמנות — כולל מוקדנים ואדמינים — חסום מהפעולות האלה.** הדפוס הנכון (fallback ל-UserPermission) כבר קיים בקוד ב-`getUsersList` ו-`linkVendorToUser` — נדרש ליישם אותו בכל הפונקציות שברשימה.

## תיקונים שהוחלו (ממתינים ל-Publish)
1. `base44/functions/syncNatiData/entry.ts` — fallback ל-UserPermission בבדיקת admin (בהיקף הדשבורד).
2. `src/pages/Dashboard.jsx` — הודעת שגיאת הסנכרון תמיד בעברית (הוסר `err.message` האנגלי; שגיאת שרת ייעודית עדיין מוצגת כלשונה).

Checkpoint ב-Base44: "QA fix: syncNatiData accepts app-level admin role + Hebrew sync error toast".

---

# סבב 7 (סופי) — סנכרון מנתיד עובד | 09/07/2026

לאחר Publish של תיקון ההרשאות, הופעל שוב "סנכרון מנתיד" כ-admin המוזמן:

| בדיקה | תוצאה |
|---|---|
| DASH-ADM-06 — סנכרון מוצלח | ✅ עבר — הודעת "סנכרון סגור: 0 נוצרו, 30 עודכנו" בעברית, בפורמט המצופה |
| הרשאות | ✅ אין יותר 403 — תיקון ה-UserPermission עובד ב-production |
| התנהגות UI | ✅ כפתור מושבת בזמן ריצה, רענון נתונים אוטומטי בסיום, ללא קריסות |

**פער לבירור מול נתי:** Dry Run הראה צבר 54 קריאות שטרם סונכרנו, אך הריצה יצרה 0 ועדכנה 30 (ייתכן עיבוד במנות מוגבלות לריצה). לוודא שהצבר יורד בריצות עוקבות.

## סטטוס סגירה — כל תסריטי הדשבורד ברי-ההרצה בוצעו
DASH-ADM-01…12 ✅ | DASH-OPR-01/02/04(חלקי)/05/06/07 ✅ | DASH-VND-01/02/03 ✅ | DASH-ALL-01…07 ✅
4 באגים אמיתיים נמצאו, תוקנו ואומתו ב-production במהלך הבדיקות.

## פתוח להמשך
1. **תיקון רוחבי של בדיקות התפקיד ב-~15 פונקציות backend** (הממצא המערכתי) — משימת פיתוח נפרדת + בדיקות regression למוקדן.
2. **DASH-OPR-03 — שלילת הרשאת יצירה**: דורש שינוי תצורת הרשאות ב-production.
3. שיוך קריאת בדיקה לספק כדי לבדוק את תצוגת הקריאות בפורטל.
4. החלפת סיסמאות שלושת משתמשי הבדיקה בסיום.
5. מול נתי: לוודא ירידת צבר בריצות סנכרון עוקבות + "לקוחות ייחודיים: 0" ב-Dry Run.

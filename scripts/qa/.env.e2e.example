# E2E test environment — copy to .env.e2e and fill in. DO NOT commit real passwords.
# Load with:  set -a; source scripts/qa/.env.e2e; set +a

# Live app URL
BASE_URL=https://nat-id-360-control-f4cb8a71.base44.app
BASE44_APP_ID=6955a04a2de0845ff4cb8a71

# The 3 invited test users (passwords provided out-of-band, never in the repo)
ADMIN_EMAIL=ifateytan298+admin@gmail.com
ADMIN_PW=
OP_EMAIL=ifateytan298+operator@gmail.com
OP_PW=
VND_EMAIL=ifateytan298+vendor@gmail.com
VND_PW=

# Vendor used for assignment in the core journey
VENDOR_NAME=E2E Test Vendor

# Cloud-sandbox proxy + browser (leave as-is inside Claude Code web env)
PROXY=${HTTPS_PROXY}
TLS_MAX=tls1.2
CHROMIUM_PATH=/opt/pw-browsers/chromium
SHOTS_DIR=./qa-shots

# Test-call safety
TEST_PHONE=0500000099
TEST_ADDR=הרצל 1, תל אביב

// Consolidated: re-export from hooks module for backward compatibility
// The hooks version has more capabilities (logStatusChange, logAssign, logExport, etc.)
export { useAuditLog } from '@/hooks/useAuditLog';

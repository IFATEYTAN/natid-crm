// Consolidated: re-export from feature module for backward compatibility
// The feature module version uses centralized queryKeys and has more capabilities
export { useVendors } from '@/features/vendors/hooks/useVendors';
